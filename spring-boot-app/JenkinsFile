pipeline {
    agent {
        label 'agent-1'
    }

    stages {
        stage ('checkout') {
            steps {
                checkout scm
            }

        }
        stage ('build with maven') {
            steps {
                sh 'ls -lthr'
                sh 'cd spring-boot-app && mvn clean package'
            }
        }
    }
}
